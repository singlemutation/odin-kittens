<h1>Kittens#edit</h1>
<%= render 'form', kitten: @kitten %>
<div>
    <%= link_to 'Delete', kitten_path(@kitten), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
  </div>
